[Service]
# Override ExecStart: single worker with memory limit (saves RAM on idle)
ExecStart=
ExecStart=/srv/sleep-prod/backend/backend/.venv/bin/celery -A celery_app worker --loglevel=info --concurrency=1 --max-memory-per-child=300000