[Service]
# Override ExecStart: autoscale 2→1 + per-child memory limit (saves RAM on idle)
ExecStart=
ExecStart=/srv/sleep-prod/backend/backend/.venv/bin/celery -A celery_app worker --loglevel=info --autoscale=2,1 --max-memory-per-child=300000